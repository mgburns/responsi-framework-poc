/**
 * Core script for Responsi theme
 *
 * This script is loaded on every front-end page requests.
 */

jQuery( document ).ready( function ( $ ) {
	// console.log( 'This runs when the document is loaded' );
});
